# CLAUDE.md

このリポジトリで作業する際のガイドです。

## 前提となる目的

会社の資産を**合法的に**、なるべく節税しながら個人（役員）の資産に移す。役員退職金は、この目的を達成するための主要な手段の一つとして位置づける。

- 「合法的に」が絶対条件。脱税・違法な租税回避は扱わない。グレーな手法を提示する場合も、リスクと根拠を明示し、最終判断は税理士・専門家に委ねる。
- 節税効果（損金算入・所得区分・分離課税など）を検討するときは、前提となる税制・要件を出典付きで記述する。

## プロジェクトの目的

上記の前提のもと、役員（取締役・監査役など）の退職金に関する規定・ツール・手順を整備する。

- 役員の退職金規定を文書にまとめる
- 退職金をシミュレーションするツールを作る
- 役員の規定を定める手順（社内での意思決定・承認フロー）を文書にまとめる
- その他、役員規定まわりで必要な文書・ツールを随時追加する

## 目的の一覧

上位目的（会社の資産を合法的に・節税して個人へ移す）の下に、個別の目的を置く。

| 目的 | 内容 | 状態 |
| --- | --- | --- |
| 目的1 | 将来的に会社資産を A・B・C で分ける（概ね 5:3:4、役員退職金が主軸） | 検討中（`docs/目的1-資産分割.md`、Google ドキュメント / スプレッドシート参照） |
| 目的2 | 会社で家を買うルールを決める（役員社宅制度） | 検討中（`docs/目的2-役員社宅.md`） |

新しい目的が出たら、この表に追記する。

## 想定する成果物

| 種別 | 内容 | 想定の形式 |
| --- | --- | --- |
| 規定文書 | 役員退職金規定の本文 | Markdown / Google ドキュメント |
| シミュレーションツール | 在任年数・報酬・功績倍率などから退職金額を試算する | スプレッドシート or スクリプト（要相談） |
| 手順書 | 役員規定を策定・改定する社内手順 | Markdown |

## 現状

- リポジトリは初期状態（このファイルのみ）。
- ディレクトリ構成・ツールの実装方式は未定。着手時にユーザーへ確認してから決める。

## 作業上の前提

- 退職金規定は**法務・税務・会社法・定款・株主総会決議**が関わる領域。数式・係数・前提条件を文書化するときは、出典（社内規定／法令／専門家の指示）を明示し、Claude が勝手に数値を発明しない。
- 金額計算ロジック（功績倍率方式など）を実装する場合は、計算式とその根拠を必ずコメント・ドキュメントに残す。
- 機密情報（個人の報酬額・氏名など）を扱う可能性があるため、外部サービスへの送信・公開リポジトリへの記載は事前確認する。

## 用語

- **退職金 / 退職慰労金**: 役員の退任時に支給する金銭。
- **功績倍率方式**: 「最終月額報酬 × 在任年数 × 功績倍率」で算定する代表的な計算方式。
- 新しい用語・係数を導入するときは、このセクションに定義を追記する。

## 進め方

- ディレクトリ構成や実装方式が必要になったら、決め打ちせずユーザーに方針を確認する。
- 規定本文・手順書は日本語で記述する。

## 情報の置き場所（どこに何があるか）

検討は主に Google ドキュメント / スプレッドシートで進めている。両方とも Google Drive のフォルダ（ID `1X6TY5kvTmOhPyHX0OvH6CIbS82JfwW_N`）に集約。リポジトリの `docs/` は検討記録。

### Google ドキュメント（ID `175dlroWkkol86mrml5Ic3Bb-fsRcHqbfvHifKoBKo7M`）

| タブ | 何が書いてあるか |
| --- | --- |
| 税理士の先生へ（相談概要） | 顧問税理士に渡す要約。目的・会社概要・関係者・背景と課題・相談事項・参考資料 |
| 資産分配プラン（全体方針） | 現行の全体方針。目的・前提・決定事項（5つ）と理由/経緯・想定役員構成・残課題・進め方 |
| プランの問題点（フラット検証） | 3観点（目的適合 / 適法性 / 現実性）の問題点・代替手段・総合評価 |
| 概要 / 必要姓 / アクション1・2 | Gemini と作った当初アクションプラン（参考。現行方針は「資産分配プラン」タブが正） |

### スプレッドシート（ID `1IxxEqqJBLvmmU8eArXgANMlPCDLdlvTFxmxgMbD2tuM`）

| シート | 何があるか |
| --- | --- |
| 報酬引き上げ計画 | 主軸の試算。各人の報酬引き上げ → 退職金（基本額 ＋ 功労加算金）→ 税・手取り → A:B:C 比率 → 年次推移 |
| 前提・税率表 | 実効法人税率・所得税速算表・退職所得控除・注意事項 |

### リポジトリ `docs/`

| ファイル | 内容 |
| --- | --- |
| `docs/前提情報.md` | 会社概要・人物・持株・資産規模・株式取得の経緯・分配比率の方針 |
| `docs/目的1-資産分割.md` | 目的1の検討記録（出口の決定、Gemini プラン検証、シミュレーションの記録） |
| `docs/目的2-役員社宅.md` | 目的2の検討記録（会社所有・退職金として現物支給・対象役員・金額上限などの方針） |

新しい文書・シートを足したら、この表を更新する。

## Google スプレッドシートを編集するときのルール

**書式（背景色・太字・数値書式）は、データを書き換えるたびに必ず一緒に調整する。** 行を追加・移動・削除すると、書式は古い位置に残ったまま新しい位置には何も付かない。結果として「データを動かしたのに2049年のセルに背景色が残っている」「変なところが太字になっている」といった不整合が起きる。

### 必須の調整項目

データを書き換えるたびに、以下を確認・再適用する:

- **背景色**: 行を動かした範囲は、まず白（`#FFFFFF`）にリセットしてから、必要な箇所（ヘッダー = `#E6E6E6`、入力欄 = `#FFF2CC`、合計行 = `#E6E6E6`）に再付与する。
- **太字**: 同様にリセット（`bold: false`）してから、ヘッダー・列見出し・合計行など意味のある場所だけ `bold: true` を付ける。
- **数値書式**: 金額の円は `#,##0`、％は `0.0%`、年は `0`。**円のセルに％書式が残ったり、％のセルに整数書式が残ったりしないよう、書き換え範囲は再適用する**。

### 手順

1. データを書き換える前後で、影響範囲（変更したセル＋その周辺の行・列）を把握する。
2. 書き換えた直後に、影響範囲を **背景色 `#FFFFFF`・太字なしでリセット**する。
3. ヘッダー・列見出し・入力欄・合計行に **再度書式を適用**する。
4. 数値書式（円・％・年）も同じ範囲に再適用する。
5. `read_sheet_values` で書き換え後の値と書式を確認する。

### よくある失敗

- 行を1行下にシフトしたら、元の行の背景色が新しい位置に残る → リセット忘れ。
- 比率セル（％）を金額セル（円）に書き換えたら、％書式のまま → 数値書式の再適用忘れ。
- 古いヘッダー位置に太字が残って、新しいヘッダーは普通の文字のまま。

データを動かしたら必ず書式も一緒に動かす、を徹底する。

## Google ドキュメントで表を作るときのルール

**`populate_from_markdown` のパイプ表（`| A | B |`）は本物の表にならない。** プレーンテキストのまま挿入される。表が必要なときは必ず `batch_update_doc` の `insert_table` でセルを作り、テキストを流し込む。マークダウン本文中にはパイプ表を書かない（書くなら後で消す前提の仮置きとして扱う）。

### 手順

1. 表以外の本文を `populate_from_markdown` で入れる。表を置く場所には目印になる短い段落を1つ置く（または後で消す仮テキスト）。
2. `inspect_doc_structure`（detailed）で、表を入れたい段落の `start_index` を特定する。仮テキストがあれば `delete_text` で消す。
3. `batch_update_doc` の `insert_table`（`rows`, `columns`, `index` 指定）で空表を挿入する。
4. `inspect_doc_structure` を再実行し、`table_details.start_index`（= T）を取得する。
5. 各セルへの流し込み index を次式で算出する（行 r・列 c はいずれも 0 始まり、C は列数）:
   - **流し込み index = T + 3 + r × (2C + 1) + c × 2**
   - 例（4列表, T=411）: (0,0)=414, (0,1)=416, (1,0)=423, (3,3)=447。
6. `insert_text` を **index の降順（後ろのセルから先頭へ）** で 1 バッチにまとめて実行する。降順なら先の挿入で後段の index がずれない。
7. `get_doc_as_markdown` か `inspect_doc_structure` で、罫線付きの表になったか必ず確認する。

### 注意

- `populate_from_markdown` を `replace_existing=true` で再実行するとタブ内の表も消える。表は本文流し込みを終えた後に作る。
- 複数の表を同じタブで作る／直すときは、**ドキュメントの後方にある表から先に処理**する。後方を編集しても前方の index がずれないため。
